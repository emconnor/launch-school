// a function that returns the sum of two numbers
// check that numbers are truthy + numbers
// return sum of two numbers
// END

// a function that takes an array of strings, and returns a string that is all those strings concatenated together
// if array is empty / null, return undefined
// SET result string to empty string
// WHILE there are elements in the array, concatenate each with result
// return result
// END

// a function that takes an array of integers, and returns a new array with every other element
// SET arrayResult to empty array
// WHILE there are elements in the array
// IF the index is 0 or % 2 === 0, add element to arrayResult
// ELSE continue
// return arrayResult
// END